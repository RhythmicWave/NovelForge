# NovelForge 部署指南（源码版）

> 适用于在其它电脑上部署 NovelForge（含能力检测自动探测 max_tokens 的修改）
> 修改所在分支: feat/auto-detect-max-tokens
> fork 仓库: https://github.com/leonluo2008-ops/NovelForge

---

## 一、前置要求

- Python 3.11（后端）
- Node.js 18+（前端，含 npm）
- git
- 一个 LLM 的 API Key（如 ollama-cloud / 聚鑫 / OpenAI 兼容）

## 二、获取代码

```bash
# 克隆你的 fork（含修改）
git clone https://github.com/leonluo2008-ops/NovelForge.git
cd NovelForge

# 切到含修改的分支
git checkout feat/auto-detect-max-tokens
```

> 如果只要官方原版，clone 原仓库即可：
> `git clone https://github.com/RhythmicWave/NovelForge.git`

## 三、后端部署

```bash
cd backend

# 1. 创建虚拟环境（推荐 uv，或 conda）
uv venv .venv --python 3.11
# 或 conda create -n novelforge python=3.11 && conda activate novelforge

# 2. 安装依赖
uv pip install -r requirements.txt --python .venv/Scripts/python.exe
# 或 pip install -r requirements.txt

# 3. 配置环境变量
cp .env.example .env
# 编辑 .env，关键项：
#   KNOWLEDGE_GRAPH_PROVIDER=sqlite   (默认 SQLite，无需 Neo4j)
#   BOOTSTRAP_OVERWRITE=true          (启动时同步内置资源)

# 4. 启动后端
.venv/Scripts/python.exe main.py
# 监听 http://0.0.0.0:54321
```

## 四、前端部署

```bash
cd frontend

# 1. 安装依赖
npm install

# 2. 启动（二选一）
npm run dev:web   # 纯浏览器访问 http://localhost:5173
npm run dev       # Electron 桌面应用
```

## 五、配置 LLM（首次必做）

打开前端 → 设置 → LLM 配置 → 新增配置：

- 提供商：OpenAI兼容 (openai_compatible)
- API Base：你的 LLM 端点（如 https://ollama.com/v1）
- API Key：你的 key
- 模型名称：如 deepseek-v4-flash:0731
- 协议：Chat 模式 (chat_completions)
- 其余留空

点"完整能力检测"验证。**修改后**：thinking 模型会自动探测合适的 max_tokens，不再误报 Empty response。

## 六、验证修改是否生效

能力检测应显示：
- 模型列表 / 基础连接 / 普通审核 / 流式输出 / 结构化输出 / 原生工具调用 → 全部"通过"
- 整体判定 full

如果基础连接仍报 Empty response，检查：
1. 是否在 feat/auto-detect-max-tokens 分支（`git branch`）
2. 后端是否重启过（代码改动需重启后端生效）
3. 模型名是否正确

## 七、常见问题

- **端口冲突**：后端 54321、前端 5173。若被占用，改 main.py 端口或杀进程。
- **Neo4j**：默认 SQLite，无需安装。想用图数据库才需 Neo4j 5.16+。
- **数据迁移**：数据在 backend/novelforge.db，复制该文件即可迁移。
- **升级**：git pull 后重启后端，BOOTSTRAP_OVERWRITE=true 自动同步内置资源。
- **Windows 注意**：用 uv 建 venv 时指定 --python 3.11，避免默认 3.14 兼容问题。

## 八、本机已验证环境（参考）

- 源码: C:\Users\leon3\NovelForge-src
- 后端 venv: backend\.venv (Python 3.11)
- 前端: frontend\node_modules
- 能力检测: deepseek-v4-flash:0731 → full（自动探测 max_tokens=1024）
